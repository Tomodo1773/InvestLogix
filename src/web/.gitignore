# dependencies
node_modules
.pnpm-store

# production
dist
build

# debug
*.log

# local env files
.env*.local

# cloudflare workers
.wrangler
# wrangler dev 用のローカル環境変数（API_ORIGIN が入るため絶対にコミットしない）
.dev.vars
.dev.vars.*
# wrangler types の生成物。14000行超あり、かつ .dev.vars の変数名を取り込むので
# マシン間で内容が一致しない。pnpm typecheck が毎回先頭で生成する
worker-configuration.d.ts

# typescript
*.tsbuildinfo
